# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
#if [ -n "$BASH_VERSION" ]; then
#    # include .bashrc if it exists
#    if [ -f "$HOME/.bashrc" ]; then
#	. "$HOME/.bashrc"
#    fi
#fi

# set PATH so it includes user's private bin if it exists
#if [ -d "$HOME/bin" ] ; then
#    PATH="$HOME/bin:$PATH"
#fi

# set PATH so it includes user's private bin if it exists
#if [ -d "$HOME/.local/bin" ] ; then
#    PATH="$HOME/.local/bin:$PATH"
#fi
#. "$HOME/.cargo/env"
#export PATH="$PATH:/usr/lib/dart/bin"
#___MY_VMOPTIONS_SHELL_FILE="${HOME}/.jetbrains.vmoptions.sh"; if [ -f "${___MY_VMOPTIONS_SHELL_FILE}" ]; then . "${___MY_VMOPTIONS_SHELL_FILE}"; fi


# leapX
#export DOCKER_BUILDKIT=1
#export COMPOSE_DOCKER_CLI_BUILD=1
#export KAFKA_URLS=127.0.0.1:9091
#export DB_CONNECTION_REFRESH_MINUTES=1
#export DEVICES_PLATFORM_APPLICATION_ARN=arn:aws:sns:us-east-1:563875027714:app/GCM/DeunaTest
#export DB_ROTATING_KEY=true
#export CLOUD_SERVICE_PROVIDER=AWS


# Added by Toolbox App
#export PATH="$PATH:/home/mayalaat/.local/share/JetBrains/Toolbox/scripts"

# ulimit -u 10000
